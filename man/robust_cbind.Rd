% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{robust_cbind}
\alias{robust_cbind}
\title{Combine Two Data Frames by Row Names, Handling Missing Rows}
\usage{
robust_cbind(df1, df2)
}
\arguments{
\item{df1}{A data frame, where row names are used for the merge.}

\item{df2}{A second data frame, also merged by row names.}
}
\value{
A data frame that merges \code{df1} and \code{df2} by row names, with missing rows handled
and rows containing \code{NA} removed.
}
\description{
This function merges two data frames by their row names, ensuring that rows missing
from either data frame are handled by filling with \code{NA} values. The merged result
is then filtered to remove rows that contain \code{NA}.
}
\examples{
df1 <- data.frame(A = c(1, 2, 3), row.names = c("row1", "row2", "row3"))
df2 <- data.frame(B = c(4, 5), row.names = c("row1", "row3"))
result <- robust_cbind(df1, df2)
print(result)
}
